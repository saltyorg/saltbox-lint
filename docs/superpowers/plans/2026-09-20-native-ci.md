# Native CI portability corrections before release tagging

Release preparation found native failures in run35488955544 at ceb96f4. Linux x64/ARM64 passed, including installed editor and musl checks; Windows and macOS failed platform assumptions in tests and Windows checkout byte conversion. Shared-actions CI35489213983 passed atf9bf1c0; local annotated v1.2.0 already prepared, not pushed. Linter v0.1.0 must wait for a passing native matrix on corrected code.

Local corrections only: no push, tag push, release, workflow rerun or consumer mutation is authorized for these changes. Retain current main checkout and exact tracked scope. Use sequential subagent implementers, independent task spec/quality reviews and final integration review. Preserve product policy, assertions and native coverage; do not skip platforms/tests to obtain green. Source case evidence/logs are in bin/ci-qualification/35488955544. Go reference checkout refreshed upstream, unchanged.

## Task 1: Make checkout and path fixtures portable

Add a repository .gitattributes policy so automatic Windows CRLF conversion cannot alter tracked assets/fixtures or source line endings. All current tracked text blobs are LF. Use default text=auto eol=lf; mark frozen highlight/assets and all testdata bytes -text so imported/purposeful bytes remain exact. Preserve binaries and explicit CRLF cases; do not renormalize source files or regenerate hashes. Reproduce using a temporary checkout/index with core.autocrlf=true before/after the policy and verify asset hashes/fixture bytes, running relevant existing tests from a temporary checkout if needed. No edits to global Git configuration.

Fix test assumptions without changing production path semantics:
- lint/discovery_test.go: canonicalize expected t.TempDir/root/file identities using filepath.EvalSymlinks; preserve intended alias-vs-canonical test inputs and all path/selection assertions. Controller reproduced failures on Linux with TMPDIR set to a symlink; reproduce same four tests and show green after fix. Adapt TestGitDirectoryCandidatesRespectPathBoundaries fixtures/inputs/expected results to native separators and sorted native paths, matching production's contract.
- lint/rules_sections_test.go: compare post-fix mode with actual pre-fix mode, rather than assuming Unix0600 works on Windows. Preserve mode preservation assertion.
- highlight/catalog/catalog_test.go: adapt captured ansible-doc fixture paths and corresponding library roots to native absolute paths in test setup; use filepath.Join(t.TempDir(),...) for synthetic library paths. Production Generate uses local Ansible paths; leave production validation intact.
- report/diff_test.go: configure each temporary Git repository core.autocrlf=false so byte-preserving patch tests control their own conversion policy. Existing failure reproduces on Linux using a temporary GIT_CONFIG_GLOBAL enabling autocrlf=true; demonstrate that test green after change. Keep actual LF, CRLF, Unicode/filename byte assertions.

Cover observed Windows format/highlight/semantics/importer fixture failures via checkout policy rather than normalizing source bytes inside product/tests. Run affected suites and controlled alias/autocrlf reproductions. Cross-compile affected tests for Windows when useful, explicitly not native execution. Commit focused fix(tests) or fix(ci) scope matching final changes. No new generic parser/filesystem abstraction.

## Task 2: Correct native process and terminal test assumptions

editor_process_windows_test.go: replace windows.InvalidHandle in invalid-process rejection test with windows.Handle(0). InvalidHandle=-1 is the valid current-process pseudo-handle, also used by production setupEditorProcess; do not reject it in production or weaken process lifetime tests. Native CI logs are RED evidence; cross-compile Windows tests and retain native rerun as required.

cmd/terminal_unix_test.go: Darwin revokes a controlling terminal when its session-leading child exits. Existing cancellation/exit/output assertions pass, then parent termios ioctl after child.Wait fails ENOTTY. Capture baseline and verify restored terminal mode inside the helper after Run returns but before os.Exit. Send an explicit verification marker/result to the parent. Keep real Ctrl+C interruption, exit status, terminal-query detection and restored-state assertions; do not skip Darwin or claim restoration solely from successful exit. Use existing platform helpers; avoid production changes. Run focused Linux PTY tests and cross-compile Darwin variants; native CI remains required.

Record exact tests/commands/output, native limits and self-review. Commit fix(tests): correct native process and terminal assumptions or equally precise Conventional Commit subject.

## Final validation and publication boundary

Run make check, controlled Git-autocrlf/alias feedback loops, applicable cross-compiles and independent integration review. Capture all job failures and keep original logs. Prepare concrete local commits and ask authorization to push corrected linter main so a new native CI run can establish readiness. Do not create v0.1.0 before that new run succeeds. No cancellation/rerun of existing workflows without authorization.

## Follow-up run 35490602808

At6202787 all six native Go suites passed. Later stages exposed two further issues: Windowsx64 console query write timeout loses errors.Is identity through %v formatting; WindowsARM64 and both macOS hosts encounter stale formatter registration after YAML-to-Ansible language change. Both Linux jobs passed. User explicitly authorized reviewed linter branch fixes/pushes needed for this same CI qualification, excluding tag pushes/releases.

## Task 3: Preserve terminal query write errors

Unify the small background-request write-result check used by cmd/terminal_unix.go and cmd/terminal_windows.go in shared cmd code. Preserve real error identity with %w, including context.Canceled and context.DeadlineExceeded. A short write without an error must be represented by io.ErrShortWrite, not a nil error formatted as text; complete successful writes return no error. Retain existing written-byte context in errors. Do not increase timeouts, skip Windows race tests, or change console mode/lifecycle behavior.

Native Windowsx64 race failure at terminal_windows_test.go:63 is RED evidence (request write times out but errors.Is DeadlineExceeded is false). Add portable regression coverage for deadline/cancel/other sentinel identity, short writes, partial write plus real error, and success; ensure both platform callers use the checked path. Establish a behavioral RED by testing the existing formatter semantics before correction. Run focused terminal tests and pinned golangci; cross-compile Windows/Darwin affected tests. Commit fix(cli): preserve terminal query write errors. Report source/root cause/testcommands/results and native limits. No new dependencies or generic I/O framework.

## Task 4: Refresh retained providers on language changes

Read-only triage proved a real transient provider-registration bug using installed VSIX with a test-only realpath gate. YAML-to-Ansible close/open uses the same TextDocument and isClosed=false; tab-owned close retains diagnostics and source ownership. open previously began only an async check, leaving the selector at the old language until identify/rememberSource completes. The existing diagnostics-presence wait is vacuous because cached findings remain.

Minimal production change: in EditorIntegration.open, clear closedTabs, synchronously emit eligibilityChanged, then initiate the existing async check. providerDocuments still requires current eligibility and already verified sourceOwners; formatting still identifies and validates its own snapshot before edits. No broad provider registration or weakened identity/root/dirty-buffer checks.

Add deterministic installed-host coverage to marker/language lifecycle tests, using the proved gate from ignored marker-probe evidence. Hold only the selected source realpath after switching language; with unchanged code automatic check reaches identity once and formatting immediately returns no edits (RED). Correct code must register the new-language provider while the automatic check is still pending: invoking formatting reaches its own identity validation, stays pending while gated, and yields the expected edit after release (GREEN). Do not make it green by sleeping, manually linting, or polling formatting until it works. Retain unmarked/root-removal/closed-source safety coverage. Gate is test-only, restores real filesystem function and releases pending work in finally; no production test hooks. Preserve exact literal-glob filename and external-root alias coverage.

Probe evidence is .superpowers/sdd/2026-09-20-native-ci/marker-probe/{regression-red,fixed-gated}.log; temporary probe sources/bundles there are diagnostic artifacts, not product edits. Run extension type/unit/format gates plus full minimum/current installed marker suites locally, distinguishing platform limits. Commit fix(vscode): refresh providers when document language changes.

## Follow-up acceptance

Sequential implementation and independent task reviews, then final integration review and make check. Rebuild local packages for installed marker tests (snapshots remain local). Push reviewed corrections under the user's explicit ongoing linter-main authorization and observe the complete new native matrix. No v0.1.0 tag until a native run succeeds at the exact candidate commit.

## Task 5: Match canonical invocation paths

The terminal error correction passed Windowsx64 race tests, and the earlier minimum-SDK language-change marker failure is cleared. Both Linux jobs passed. Both Windows jobs next failed save-scope's raw document.uri.fsPath substring assertion. The real CLI is intentionally invoked with canonical snapshot.filename from realpath, while URI paths can have another drive spelling/case or alias. The recording fixture writes raw PID plus joined argv, not JSON.

In extension/test/host/save-scope.ts, compare the recorded saved-file invocation with await realpath(document.uri.fsPath). In extension/test/host/queue.ts, compute that same canonical expected filename before any close/root/disposal invalidation and use it in both positive and negative invocation assertions. Preserve exact filenames, call counts, cancellation, and diagnostic/action assertions. Do not lowercase arbitrary argv, change production identity behavior, or alter recording encoding. These are test-only corrections; original native CI provides RED. Verify canonical-vs-alias/drive spelling with a focused reproducible probe, then run type/unit/format and focused/full local installed save-scope and queue host suites. Native CI still required. Commit fix(tests): compare canonical editor invocation paths.

macOSARM64 has a separate current-SDK root-override formatter-empty assertion, and macOSx64 reports a save-echo count race. Their exact causes are still under investigation; do not guess a production fix, retry formatting until it succeeds, loosen assertions, or increase delays. Preserve the original failures and gather discriminating evidence before correcting them.
