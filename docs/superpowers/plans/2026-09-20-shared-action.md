# Move the reusable linter Action to shared actions

User requested the Action move to /opt/git/github-actions. Implement locally in the existing clean main checkouts using sequential subagent-driven implementation and independent review. No pushes, tags, releases, remote workflow runs, or Saltbox/Sandbox consumer edits. Existing binary releases remain owned by saltyorg/saltbox-lint; no changes to lint/editor behavior or release versions.

## Task 1: Add the Action to the shared repository

Work in /opt/git/github-actions. Add saltbox-lint/action.yml, install.sh and run.sh, moving the existing implementation from /opt/git/saltbox-lint/action.yml and action/{install,run}.sh. This task copies it first; Task2 removes the original after review. Adapt only packaged script paths for the new directory. Preserve existing inputs (required exact stable version; working-directory and newline paths defaults), Linux X64/ARM64 support, checksum and binary-version validation, loopback-only fixture override, exit0/1/2, literal path handling and no implicit fixes. Keep download host saltyorg/saltbox-lint. Do not add Go or new Python dependencies to shared actions.

Port shell-boundary coverage from linter action/action_test.go into shared Python stdlib unittest tests. Exercise actual packaged inline run commands and Bash scripts using a local HTTP archive fixture, exact installer bytes, malformed/duplicate/missing checksums, version/platform failures before download, corrupt/malformed archives, wrong binary versions, invalid download override, launch failures, working directories/spaces/CRLF/multiple/metacharacter/option-like literal paths and exit statuses. Use a small executable argv-recording fixture for wrapper-only tests, without claiming that it implements linter policy. Add an optional real-binary integration test selected by SALTBOX_LINT_TEST_BINARY pointing to a local saltbox-lint version1.2.3 executable. That test installs that actual executable through loopback HTTP, checks real findings/annotations/summary, and verifies source bytes cannot be fixed by option-like paths. Default shared CI must work independently of a sibling checkout/unpublished linter release; controller must run real-binary mode locally before completion. Document this explicit boundary.

Extend existing tests/actionlint/actions.yml with ./saltbox-lint and add bash syntax validation to existing CI. Update README to show uses: saltyorg/github-actions/saltbox-lint@<full-commit-sha>, independent binary version pin, dependencies/inputs/outputs/exit contract. Refresh .agents/stack-context.md for Bash/action tests and added gate without changing existing retry/notify implementations or public interfaces. Match repository conventions and GPL source attribution.

TDD: first port installer/entrypoint tests and demonstrate missing Action failure, then add runtime files and complete behavioral cases. Run python3 -m unittest discover -v, compileall, bash syntax, existing pinned actionlint validation, git diff --check. Stage exact files and commit feat(saltbox-lint): add reusable linter action. Report exact commands, outcomes, ported coverage and real-binary test invocation (controller owns final cross-repo integration).

## Task 2: Remove the old Action and update linter references

Work in /opt/git/saltbox-lint after Task1 is accepted. Remove root action.yml and action/install.sh, action/run.sh, action/action_test.go now owned by shared repository. Preserve unrelated Makefile/VS Code task coverage by moving action/{format_test.go,helpers_test.go,vscode_test.go} to integration/ with package integration_test; preserve their behavior and cross-platform build constraints.

Remove the Bash syntax gate from linter Makefile now owned by shared CI. Keep linter CI and release workflows here. Update README links/usage, all four examples/github templates, docs/rule-migration.md and current stack/release guidance to refer to saltyorg/github-actions/saltbox-lint. Use ACTION_COMMIT_SHA as template placeholder, explicitly describing shared-action commit and linter binary version as independent pins. Keep examples illustrative, no invented published version/SHA and no edits to actual Saltbox/Sandbox workflows. Preserve historical qualification/research documents as historical records rather than rewriting old evidence.

Run moved integration tests, make check, and search active docs/examples/gates for stale references to the removed action paths. Update stack context to explain test ownership. Stage exact paths and commit refactor(action): move reusable action to shared repository. Record task evidence.

## Final integration acceptance

Independent spec/code-quality task reviews and a final cross-repository review. Build local version1.2.3 linter into ignored plan workspace, run all shared Python tests with SALTBOX_LINT_TEST_BINARY set so actual installer/run behavior is exercised. Run each repo's existing non-mutating quality gates and relevant actionlint example checks; verify clean worktrees and Conventional Commit subjects. Document final local commits and usage. No stable artifact/publication claim follows from moving the action; previously recorded snapshot artifacts predate this change.
