example_role_map:
  subdomain: "{{ lookup('role_var', '_api_subdomain', role='other') }}"
  domain: "{{ lookup('role_var', '_api_domain', role='other') }}"
example_role_seq:
  - "{{ lookup('role_var', '_api_subdomain', role='other') }}"
  - "{{ lookup('role_var', '_api_domain', role='other') }}"
example_role_dict: "{{ {'subdomain': lookup('role_var', '_api_subdomain', role='other'), 'domain': lookup('role_var', '_api_domain', role='other')} }}"
example_role_list: "{{ [lookup('role_var', '_api_subdomain', role='other'), lookup('role_var', '_api_domain', role='other')] }}"
example_role_conditional: "{{ lookup('role_var', '_api_subdomain', role='other') if enabled else lookup('role_var', '_api_domain', role='other') }}"
example_role_conditional_concat: "{{ lookup('role_var', '_api_subdomain', role='other') ~ '.' if enabled else '.' ~ lookup('role_var', '_api_domain', role='other') }}"
example_role_separate_outputs: "{{ lookup('role_var', '_api_subdomain', role='other') }}.{{ disabled }}.{{ lookup('role_var', '_api_domain', role='other') }}"
example_role_space_before_dot: "{{ lookup('role_var', '_api_subdomain', role='other') }} .{{ lookup('role_var', '_api_domain', role='other') }}"
example_role_space_after_dot: "{{ lookup('role_var', '_api_subdomain', role='other') }}. {{ lookup('role_var', '_api_domain', role='other') }}"
example_role_space_around_dot: "{{ lookup('role_var', '_api_subdomain', role='other') }} . {{ lookup('role_var', '_api_domain', role='other') }}"
example_role_calls: "{{ format_host(lookup('role_var', '_api_subdomain', role='other'), lookup('role_var', '_api_domain', role='other')) }}"
example_role_other: "{{ lookup('role_var', '_api_subdomain', role='one') }}.{{ lookup('role_var', '_api_domain', role='two') }}"
example_role_dynamic: "{{ lookup('role_var', suffix, role='other') ~ lookup('role_var', '_api_domain', role='other') }}"
