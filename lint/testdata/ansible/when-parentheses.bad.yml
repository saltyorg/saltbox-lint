- name: Check nontrivial conditions
  ansible.builtin.debug:
    msg: ok
  when:
    - value is defined
    - value | bool
    - (a) or (b)
    - values[index + 1]
    - lookup('role_var', '_metrics_enabled', role='traefik') | bool
